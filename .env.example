# ================================
# Supabase 配置（后端）
# ================================
# 获取位置：Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-public-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ================================
# Supabase 配置（前端 - 必须与后端一致）
# ================================
# VITE_ 前缀用于 Vite 构建时注入到前端代码
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# ================================
# 数据库连接（Drizzle ORM）
# ================================
# 获取位置：Supabase Dashboard → Settings → Database → Connection String
# ⚠️ 必须使用 Transaction Pooler（端口 6543），不要使用 Direct connection（端口 5432）
# 格式：postgres://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=postgres://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres

# ================================
# AI 服务配置
# ================================
# OpenRouter API（支持多模型）
# 获取位置：https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 默认 AI 模型（聊天助手、通用任务）
AI_MODEL=google/gemini-2.5-flash

# 简历分析专用模型（需要高准确度）
RESUME_AI_MODEL=openai/gpt-5

# 候选人画像生成模型
PROFILE_AI_MODEL=openai/gpt-5

# 职位匹配模型（平衡效果和成本）
MATCHING_AI_MODEL=google/gemini-2.5-pro

# 聊天助手模型（高频调用，经济型）
CHAT_AI_MODEL=google/gemini-2.5-flash

# 视觉解析模型（PDF 多模态分析）
VISION_AI_MODEL=openai/gpt-5

# 功能开关：启用视觉解析（true/false）
ENABLE_VISION_PARSING=true

# ================================
# 服务器配置
# ================================
# 运行环境（development/production）
NODE_ENV=development

# 服务器端口
PORT=5000

# Session 密钥（⚠️ 生产环境必须修改为随机字符串）
# 使用以下命令生成：openssl rand -base64 32
SESSION_SECRET=replace-me-with-random-string

# CORS 允许的源
# 开发环境：允许本地开发服务器
# 生产环境：必须修改为实际域名（如 https://your-app.vercel.app）
CORS_ORIGIN=http://localhost:5000,http://localhost:5173

# ================================
# 可选配置
# ================================
# 日志级别（debug/info/warn/error）
LOG_LEVEL=info

# 文件上传大小限制（MB）
MAX_FILE_SIZE=10

# AI Token 追踪（启用后会记录 API 使用量）
ENABLE_AI_TOKEN_TRACKING=true
