# Supabase 配置（后端）
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase 配置（前端 - 需要 VITE_ 前缀）
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# 数据库连接字符串（用于 Drizzle ORM）
# 格式: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
# 在 Supabase Dashboard -> Settings -> Database -> Connection String 中获取
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# OpenRouter AI 配置
OPENROUTER_API_KEY=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

# 推荐使用的模型配置
# openai/gpt-5 - 最强旗舰模型（推荐用于简历分析和画像生成）
# openai/gpt-5-chat - 旗舰对话模型
# anthropic/claude-sonnet-4 - 备用旗舰
# google/gemini-2.5-pro - 推理型
# google/gemini-2.5-flash - 经济型
# deepseek/deepseek-v3.1-terminus - 经济型

# 不同功能使用不同模型（可根据需要调整）
RESUME_AI_MODEL=openai/gpt-5          # 简历分析（需要最高准确度）
PROFILE_AI_MODEL=openai/gpt-5         # 画像生成（需要深度理解）
MATCHING_AI_MODEL=google/gemini-2.5-pro    # 职位匹配（平衡效果和成本）
CHAT_AI_MODEL=google/gemini-2.5-flash      # 聊天助手（频繁调用，使用经济型）
AI_MODEL=google/gemini-2.5-pro             # 默认模型

# 视觉分析配置
VISION_AI_MODEL=openai/gpt-5          # 视觉分析模型（GPT-5 支持多模态）
ENABLE_VISION_PARSING=true            # 启用视觉解析（PDF 会转为图片用 GPT-5 分析）

# Node 环境
NODE_ENV=development

# Session 密钥（生产环境需要改为随机字符串）
SESSION_SECRET=your-secret-key-change-in-production